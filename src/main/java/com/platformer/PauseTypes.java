package com.platformer;

/**
 * Enum to record the reason for pause.
 *
 * @author Samuel Zhang
 */
public enum PauseTypes {

    LEVEL_PAUSE, // if a level was paused
    FINISH_PAUSE, // if a level was finished
    SAVE_PAUSE, // if saving data

}